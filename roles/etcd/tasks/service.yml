- name: template /etc/systemd/system/etcd.service
  template:
    src: etcd.service.j2
    dest: /etc/systemd/system/etcd.service
  become: yes
  notify:
    - etcd-reload-restart

- name: enable/start etcd.service
  service:
    name: etcd
    state: started
    enabled: yes
  become: yes
